{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "screenshot-viewer"],
  "permissions": [
    "core:default",
    "opener:default",
    "window-state:default"
  ]
}
